program
	
	record point 
	begin
		x: Integer;
		y: Integer;
	end;
	q:point;
	record threedpoint 
	begin
		x: Integer;
		z: Integer;
		y: Integer;
	end;
	k: Integer;
	x: Integer;
	donothing(x : Integer) : Integer is
	y: Integer;
	begin
	 	x := (x + 1);
		if (x > 3) then
		x := 3;
		return 3;
		else 
		x :=5; 
		return 3;
		end if;
	end ;


begin
	print "Howdy!";
	k := donothing(q.x);
end;
