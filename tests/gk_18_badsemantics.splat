program
	
	record point 
	begin
		x: Integer;
		y: Integer;
	end;
	k: Integer;
q:point;
	donothing(x : Integer) : Integer is
	y: Integer;
	begin
	 	x := (x + 1);
		if (x > 3) then
		x := 3;

		else 
		x :=5; 
		return x;
		end if;
	end ;


begin
	print "Howdy!";
	k := donothing(q.x);
end;
