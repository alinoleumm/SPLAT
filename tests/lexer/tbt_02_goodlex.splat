var a, b:integer;
begin
    assign(input, "input.txt"); reset(input);
    assign(output, "output.txt"); rewrite(output);

    readln(a);
    readln(b);

    if (a > b) then
        begin
            writeln("Correct");
        end;

    close(input);
    close(output);
end.;