program
	
	record point 
	begin
		x: Integer;
		y: Integer;
	end;
	q:point;
	k: Integer;
	donothing(x : Integer) : Integer is
	y: Integer;
	begin
	 	x := (x + 1);
		return x;
	end ;
	donothing2(x : Integer) : Integer is
	begin
		x := (x + 1);
		return x;
	end ;
begin
	print "Howdy!";
	k := donothing(q.z);
end;
