program
	
	record point 
	begin
		x: Integer;
		y: Integer;
	end;
	k: Integer;
	q:point;
	donothing(x : Integer) : Integer is
	y: Integer;
	begin
	 	x := (x + 1);
		return ;
	end ;

begin
	print "Howdy!";
	k := donothing(q.x);
end;
